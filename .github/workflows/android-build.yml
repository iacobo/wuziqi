name: Build Android APK

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          java-version: '17'   # Use JDK 17
          distribution: 'adopt'

      - name: Cache Gradle packages
        uses: actions/cache@v3
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Make gradlew executable
        run: chmod +x ./gradlew  # Add this step to change permissions

      - name: Download Dependencies
        run: ./gradlew dependencies -Dorg.gradle.wrapper.disable=true # Disable wrapper prompts

      - name: Build APK
        run: ./gradlew assembleDebug -Dorg.gradle.wrapper.disable=true # Disable wrapper prompts

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: gomoku-apk
          path: app/build/outputs/apk/debug/app-debug.apk
